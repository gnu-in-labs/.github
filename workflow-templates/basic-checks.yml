name: Basic checks

# GNU.IN starter workflow.
# Replace the placeholder step with the repository's real lint, build,
# and test commands. Automation should leave evidence: keep the checks
# honest about what is and is not verified.

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

permissions:
  contents: read

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run repository checks
        run: |
          echo "Replace this step with this repository's lint, build, and test commands."
          echo "A green run of a placeholder is not verification."
          exit 0
